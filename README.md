# RAG

```bash
pip install -r requirement.txt
source venv/bin/activate
cd crawler
python {doc}_doc.py
```

## Retrieval 

To submit an query to spark/numpy, go to the scripts folder and find the corresponding retrieval script.
